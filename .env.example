# Base URL of the e-JOBS Spring backend, including the /api prefix.
# NEXT_PUBLIC_* values are inlined at BUILD time — this must be set before
# `next build` runs, not just at runtime.
NEXT_PUBLIC_API_URL=https://e-jobs-6npq.onrender.com/api
